/**
 * Stub for future Web Worker implementation of dagre layout.
 * Currently unused — layout runs synchronously in graphUtils.ts.
 *
 * To enable: import dagre here, call runLayout, post result back to main thread.
 * Requires a webpack worker-loader configuration compatible with the JupyterLab builder.
 */
export {};
